// URL, connection
export const baseUrl = 'http://localhost:8010'
export const tokenPrefix = 'Bearer ';

// customer url
export const customerUrl = baseUrl + '/customer';
export const customerHelloUrl = customerUrl + '/hello';

// user url
export const authenticateUserUrl = baseUrl + '/authenticate';
export const createUserUrl = baseUrl + '/user';
